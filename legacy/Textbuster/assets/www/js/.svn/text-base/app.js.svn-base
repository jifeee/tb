/*
 * Load a value from the permant storage
 */
function loadValue(key, def){
	var val = window.localStorage.getItem(key);
	return (val) ? val : (def) ? def : null;
};

Ext.regApplication({
	name: 'app',
    launch: function() {
      
       app.views.viewport = new app.views.Viewport();
       app.views.service = new app.views.Menu();
       
   },
   init: function() {
	try{
	   
	}catch(e){alert(e.stack)}
   }
}); 
