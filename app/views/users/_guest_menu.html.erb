<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <%= brand(false) %>
      <div class="nav-block">
        <ul class="nav">
          <% Page.by_menu(:menu_logged_out).each do |page| %>
            <li><%= link_to page.title, page_path(page) %></li>
          <% end %>
          <li><%= link_to "Register", new_user_registration_path %></li>
          <li><%= link_to "Login", new_user_session_path, :class => 'alt' %></li>
        </ul>
      </div>
    </div>
  </div>
</div><!-- /navbar -->

